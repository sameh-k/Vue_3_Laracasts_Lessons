{
  "scripts": {
    "frontend": "npx serve",
    "fake": "npx json-server --watch db.json -p 3001",
    "start": "npm run fake & npm run frontend"
  },
  "devDependencies": {
    "json-server": "^1.0.0-alpha.23"
  }
}
